# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MovieToGo' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for MovieToGo
  pod 'Firebase/Core'
  pod 'Firebase/AdMob'

  target 'MovieToGoTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
